from distutils.core import setup
setup(
      name = 'Green Translator',
      packages = ['greentranslator'], # this must be the same as the name above
      version = '0.1',
      description = 'Green Team BioMedical Data Translator',
      author = 'Steve Cox',
      author_email = 'scox@renci.org',
      url = 'https://github.com/ResearchSoftwareInstitute/greendatatranslator', # use the URL to the github repo
      download_url = 'https://github.com/ResearchSoftwareInstitute/greendatatranslator/archive/0.1.tar.gz', # I'll explain this in a second
      keywords = ['biomedical'], # arbitrary keywords
      classifiers = [],
    )
